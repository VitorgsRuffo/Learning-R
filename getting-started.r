#creating an object variable and assigning a value to it:
    x = 10
    y <- 20
    z = "Hello world"
    w <- "11" 

#printing the value of an object:
    print(x)
    print(y)
    print(z)

#command for checking all the objects stored in the memory:
    ls()

#removing an object from the workspace memory:
    rm(y)


#operations:
    a = 5
    b = 3

    c = a + b
    d = a - b
    e = a * b
    f = a / b

    g = b^3 #b³
    h = sqrt(9) #square root
    h2 = g^(1/3) #cube root

    i = log(a) #natural logarithm of a (i.e, of base e).
    i2 = log(10, 10) #the secong parameter specifies a base different from the e (that is the default base).
    j = exp(b) #e^b
    l = abs(-15) #absolute value